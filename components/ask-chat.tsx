'use client'; import { useState } from 'react'; import Link from 'next/link';
type Source = { id: string; title: string; summary: string };
export default function AskChat() { const [question, setQuestion] = useState(''); const [answer, setAnswer] = useState(''); const [sources, setSources] = useState<Source[]>([]); const [busy, setBusy] = useState(false); async function ask(event: React.FormEvent) { event.preventDefault(); setBusy(true); setAnswer(''); setSources([]); const response = await fetch('/api/ask', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ question }) }); if (!response.ok || !response.body) { setAnswer(await response.text()); setBusy(false); return; } const reader = response.body.getReader(); const decoder = new TextDecoder(); let first = true; while (true) { const { done, value } = await reader.read(); if (done) break; const text = decoder.decode(value, { stream: true }); if (first) { const newline = text.indexOf('\n'); if (newline >= 0) { try { setSources(JSON.parse(text.slice(0, newline)).sources); } catch {} setAnswer((old) => old + text.slice(newline + 1)); first = false; } } else setAnswer((old) => old + text); } setBusy(false); } return <div className="space-y-5"><form onSubmit={ask} className="panel flex gap-2"><input className="min-w-0 flex-1" value={question} onChange={(e) => setQuestion(e.target.value)} placeholder="What have I saved about…?" required /><button disabled={busy}>{busy ? 'Thinking…' : 'Ask'}</button></form>{answer && <section className="panel"><h2 className="font-semibold text-cyan-200">Answer</h2><p className="mt-3 whitespace-pre-wrap leading-7">{answer}</p>{sources.length > 0 && <div className="mt-5 border-t border-slate-800 pt-4"><p className="muted mb-2">Sources searched</p>{sources.map((source) => <Link key={source.id} href={`/items/${source.id}`} className="block py-1 text-sm text-cyan-300 hover:underline">{source.title}</Link>)}</div>}</section>}</div>; }

