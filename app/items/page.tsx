import Link from 'next/link'; import { createClient } from '@/lib/supabase/server';
export default async function ItemsPage({ searchParams }: { searchParams: { q?: string; category?: string } }) { const supabase = await createClient(); let query = supabase.from('items').select('*').order('created_at', { ascending: false }); if (searchParams.category) query = query.eq('para_category', searchParams.category); if (searchParams.q) query = query.or(`title.ilike.%${searchParams.q}%,summary.ilike.%${searchParams.q}%,raw_content.ilike.%${searchParams.q}%`); const { data: items } = await query; return <><h1 className="text-3xl font-bold">All items</h1><form className="my-6 flex flex-wrap gap-2"><input name="q" defaultValue={searchParams.q} placeholder="Search your brain" /><select name="category" defaultValue={searchParams.category}><option value="">All categories</option>{['project','area','resource','archive'].map((c) => <option key={c}>{c}</option>)}</select><button>Filter</button></form><div className="space-y-3">{items?.map((item) => <Link href={`/items/${item.id}`} key={item.id} className="block rounded-xl border border-slate-800 p-4 hover:border-cyan-600"><div className="flex items-center justify-between gap-4"><h2 className="font-semibold">{item.title}</h2><span className="tag">{item.para_category}</span></div><p className="muted mt-2">{item.summary}</p><div className="mt-3 flex gap-1">{item.tags?.map((tag: string) => <span className="tag" key={tag}>{tag}</span>)}</div></Link>)}</div></>; }
